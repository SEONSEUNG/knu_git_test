#include <iostream>
using namespace std;

/*
class Circle {
public:
	double getArea();
	int innerRadius, outerRadius;
};

double Circle::getArea() {

	return (3.14 * outerRadius * outerRadius) - (3.14 * innerRadius * innerRadius);

}

int main() {

	Circle donut_two;
	cout << "Enter innerRadius >> ";
	cin >> donut_two.innerRadius;
	cout << "Enter outerRadius >> ";
	cin >> donut_two.outerRadius;

	double area = donut_two.getArea();

	cout << "Area: " << area << endl;

}
*/